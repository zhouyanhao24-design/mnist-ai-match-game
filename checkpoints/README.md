# Checkpoints Directory

Trained model files are saved here by default:

```text
checkpoints/resnet_mnist.pth
checkpoints/vgg_mnist.pth
```

Large model files are ignored by git. Train the models locally before running the game.
